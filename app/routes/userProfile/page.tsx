import ProfileCard from '@/components/userInformation/profile';
import React from 'react';

const page = () => {
    return (
        <div>
          <ProfileCard/>  
        </div>
    );
};

export default page;