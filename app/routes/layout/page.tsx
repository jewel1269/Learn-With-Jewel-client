import CourseLayout from '@/components/courseLayout/courseLayout';
import React from 'react';

const page = () => {
    return (
        <div>
            <CourseLayout/>
        </div>
    );
};

export default page;