import Register from '@/authentication/register/register';
import React from 'react';

const page = () => {
    return (
        <div>
            <Register/>
        </div>
    );
};

export default page;